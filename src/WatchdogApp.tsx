/**
 * Deprecated: the Watchdog page became a report inside the Reporting hub.
 * Kept only because the sandbox can't delete files; re-exports the new
 * component so nothing dangles. Not referenced by any entry point.
 */
export { WatchdogReport as default } from "./reports/WatchdogReport";
